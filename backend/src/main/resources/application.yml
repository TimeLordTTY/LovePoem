server:
  port: 8080

spring:
  application:
    name: half-poem-backend
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:9009/poem?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&initialTimeout=1&connectTimeout=30000&socketTimeout=30000&useLocalSessionState=true&elideSetAutoCommits=true&alwaysSendSetIsolation=false&enableQueryTimeouts=false&cacheDefaultTimezone=false&cacheServerConfiguration=true&maintainTimeStats=false&netTimeoutForStreamingResults=0&tcpKeepAlive=true&allowPublicKeyRetrieval=true
    username: poem
    password: mbRckCN4m5zAR3E2
    hikari:
      maximum-pool-size: 15
      minimum-idle: 8
      connection-timeout: 20000          # 连接超时：20秒
      idle-timeout: 300000               # 空闲超时：5分钟
      max-lifetime: 600000               # 最大生命周期：10分钟
      validation-timeout: 3000           # 连接验证超时：3秒
      connection-test-query: SELECT 1    # 连接测试查询
      leak-detection-threshold: 60000    # 连接泄漏检测：60秒
      keepalive-time: 60000              # 保活时间：60秒
      connection-init-sql: SELECT 1      # 连接初始化SQL

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      update-strategy: not_empty
  mapper-locations: classpath*:/mapper/**/*.xml

# JWT配置
jwt:
  secret: ${JWT_SECRET:love-poem-jwt-secret-key-2024-baiqin-xiao-half-poem-website-secure-token-generation-key-for-authentication-system}
  expiration: 604800 # 7天，单位秒

# 应用配置
app:
  upload:
    path: uploads
    url-prefix: /uploads
    max-size: 5242880 # 5MB

# 日志配置
logging:
  level:
    com.herpoem.site: debug
    org.springframework.security: debug
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# 跨域配置
cors:
  allowed-origins: 
    - "http://localhost:*"
    - "http://127.0.0.1:*"
    - "http://124.220.229.91*"
    - "https://your-domain.com"
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
  allowed-headers: "*"
  allow-credentials: true
